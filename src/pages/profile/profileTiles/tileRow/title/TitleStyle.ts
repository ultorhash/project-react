import styled from 'styled-components';

export const StyledTitle = styled.div`
    display: flex;
    width: 100%;
    height: auto;
    background-color: transparent;
    margin: 0 1%;

    p {
        color: gray;
        font-weight: 400;
        background-color: transparent;
    }
`;