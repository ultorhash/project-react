import styled from 'styled-components';

export const StyledEntitiesFilters = styled.div`
    width: 100%;
    padding: 1%;
    background-color: transparent;
    box-shadow: 0 1px 1px gray;
    border-radius: 10px;
    margin-top: 10px;

    p {
        color: gray;
        font-size: 12px;
        background-color: transparent;
    }
`;